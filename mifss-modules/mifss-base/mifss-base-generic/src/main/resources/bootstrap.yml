spring:
  application:
    name: mifss-base-generic
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: hend
        group: DEFAULT_GROUP
        file-extension: yaml
        prefix: mifss-base-generic

